# create @App macro

like with @View where it also adds View conformance

we should just have same for @App
and then 

```swift
@App
struct MyApp {
    ...
}

// generated by macro for Apple Platforms
@main 
public struct RunApp { // or whatever it needs to generate for normal desktops / apple platforms
    public static func main() {
        // execute what needs to be done for MyApp
        // on apple platforms
    }
}

// generate something else for Android if needed...

```

# NucleantApplication
* NucleantSwiftUI-AndroidTests/ksproject_demo/swift/Sources/CAndroidNativeWindow

seems more to belong in NucleantApplication
add Swift-Java dependency
but just do so only the android related targets uses it..
move java files or whatever is needed
soo NucleantApplication has MainActivity etc
just so android demo package only needs to import NjcleantSwiftUI
and do its @App


# demo swift package 
since CAndroidNativeWindow will be moved to NucleantApplication
and swift-java etc then.
we should only need NucleantSwiftUI-AndroidTests/ksproject_demo/swift/Sources/NucleantMain/NucleantSwiftUIDemo.swift

